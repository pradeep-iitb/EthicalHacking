# Resoving apt errors

```
gedit /etc/apt/sources.list  
ping google.com
apt update
```

```
apt install --fix-broken
apt install --fix-missing
```

```
apt clean
apt update
```

```
rm -rf /var/lib/apt/lists/*
apt update
```

```
dpkg -a --configure
```
